[project]
name = "mlops"
revision = "1.0"

[profiles.default]
DATABASE_URL         = { description = "PostgreSQL DSN", required = true }
AWS_ACCESS_KEY_ID    = { description = "MinIO / S3 access key", required = true }
AWS_SECRET_ACCESS_KEY = { description = "MinIO / S3 secret key", required = true }
OPENAI_API_KEY       = { description = "OpenAI API key for LLM calls", required = false }
WANDB_API_KEY        = { description = "Weights & Biases tracking key", required = false }
