# devenv working state
.devenv/
.direnv/

# Machine-local overrides
devenv.local.nix
devenv.local.yaml

# Generated by claude.code.*: symlinks into the Nix store, regenerated on
# every shell entry.
.mcp.json
.claude/settings.json

# Generated by git-hooks
.pre-commit-config.yaml

# Commit devenv.lock in a real project; it is ignored here because this
# directory is a skill example, not a checked-out application.
devenv.lock
